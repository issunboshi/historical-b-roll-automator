---
phase: 01-enrichment-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/enrich_entities.py
  - tests/test_enrich_entities.py
autonomous: true

must_haves:
  truths:
    - "Priority score calculated from entity type, mention count, and position"
    - "People type gets highest base weight (1.0)"
    - "Multiple mentions boost score with diminishing returns"
    - "Early mentions (first 20%) get 1.1x boost"
    - "Score capped at 1.2 maximum"
  artifacts:
    - path: "tools/enrich_entities.py"
      provides: "Priority scoring functions"
      exports: ["calculate_priority", "TYPE_WEIGHTS", "mention_multiplier", "position_multiplier"]
    - path: "tests/test_enrich_entities.py"
      provides: "Priority scoring tests"
      min_lines: 50
  key_links:
    - from: "tools/enrich_entities.py"
      to: "entities_map.json structure"
      via: "entity dict with entity_type, occurrences[]"
      pattern: "entity\\.get\\(['\"]entity_type['\"]"
---

<objective>
Create the priority scoring logic for entity enrichment using TDD.

Purpose: Priority scores (0.0-1.2) will later be used in Phase 3 to filter low-value entities. This plan establishes the scoring formula based on entity type, mention count, and transcript position.

Output: `tools/enrich_entities.py` with priority scoring functions, validated by tests.
</objective>

<execution_context>
@/Users/cliffwilliams/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cliffwilliams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-enrichment-foundation/01-CONTEXT.md
@.planning/phases/01-enrichment-foundation/01-RESEARCH.md
@tools/srt_entities.py
</context>

<feature>
  <name>Priority Scoring</name>
  <files>tools/enrich_entities.py, tests/test_enrich_entities.py</files>
  <behavior>
    Priority score = base_type_weight * mention_multiplier * position_multiplier, capped at 1.2

    Type weights (from CONTEXT.md):
    - people: 1.0 (highest)
    - events: 0.9
    - organizations: 0.7
    - concepts: 0.6
    - places: 0.3 (lowest)

    Mention multiplier (diminishing returns):
    - 1 mention: 1.0x
    - 2 mentions: 1.3x
    - 3 mentions: 1.5x
    - 4+ mentions: 1.6x

    Position multiplier:
    - First mention in first 20% of transcript: 1.1x
    - Otherwise: 1.0x

    Test cases:
    - Person, 1 mention, early (20%): 1.0 * 1.0 * 1.1 = 1.1
    - Person, 4 mentions, early: 1.0 * 1.6 * 1.1 = 1.76 -> capped to 1.2
    - Place, 1 mention, late: 0.3 * 1.0 * 1.0 = 0.3
    - Event, 2 mentions, early: 0.9 * 1.3 * 1.1 = 1.287 -> capped to 1.2
    - Concept, 3 mentions, late: 0.6 * 1.5 * 1.0 = 0.9
    - Unknown type defaults to 0.5 base weight
  </behavior>
  <implementation>
    1. Create tests/test_enrich_entities.py with pytest tests for all scoring cases
    2. Create tools/enrich_entities.py with:
       - TYPE_WEIGHTS dict mapping entity types to base weights
       - srt_time_to_seconds(timecode: str) -> float helper
       - mention_multiplier(mention_count: int) -> float
       - position_multiplier(first_position_pct: float) -> float
       - calculate_priority(entity: dict, transcript_duration_seconds: float) -> float
    3. Entity dict expected structure: {"entity_type": str, "occurrences": [{"timecode": "HH:MM:SS,mmm", ...}]}
    4. Reuse srt_time_to_seconds pattern from srt_entities.py (line 237-245)
  </implementation>
</feature>

<verification>
```bash
# Run tests
cd /Users/cliffwilliams/code/davinci-b-roll-automater
python -m pytest tests/test_enrich_entities.py -v

# Verify module imports correctly
python -c "from tools.enrich_entities import calculate_priority, TYPE_WEIGHTS; print('OK')"
```
</verification>

<success_criteria>
- All priority scoring tests pass
- calculate_priority returns float in range 0.0-1.2
- TYPE_WEIGHTS contains all 5 entity types
- Edge cases handled: empty occurrences, unknown type, zero duration
</success_criteria>

<output>
After completion, create `.planning/phases/01-enrichment-foundation/01-01-SUMMARY.md`
</output>
